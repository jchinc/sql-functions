const MILLISECONDS_PER_SECOND: number = 1000;
const MILLISECONDS_PER_MINUTE: number = 60000;  // 1000 * 60
const MILLISECONDS_PER_HOUR: number = 3600000;  // 1000 * 60 * 60
const MILLISECONDS_PER_DAY: number = 86400000;  // 1000 * 60 * 60 * 24

export {
	MILLISECONDS_PER_SECOND,
	MILLISECONDS_PER_MINUTE,
	MILLISECONDS_PER_HOUR,
	MILLISECONDS_PER_DAY
};